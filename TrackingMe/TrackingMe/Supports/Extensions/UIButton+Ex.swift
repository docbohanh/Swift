//
//  UIButton+Ex.swift
//  OneTaxi
//
//  Created by Nguyen Duc Nang on 8/23/16.
//  Copyright © 2016 Hoan Pham. All rights reserved.
//

import UIKit
/*
extension UIButton {
    convenience init(style: AppConfig.ButtonStyle,
                     text: String? = nil,
                     icon: UIImage? = nil,
                     tag: Int = 0,
                     titleEdgeInsets: UIEdgeInsets = UIEdgeInsets(top: 0, left: 0, bottom: 0, right: 0),
                     imageEdgeInsets: UIEdgeInsets = UIEdgeInsets(top: 0, left: 0, bottom: 0, right: 0)) {
        
        self.init()
        
        clipsToBounds       = false
        backgroundColor = style.backgroundColor
        setTitleColor(style.textColor, for: UIControlState())
        titleLabel?.font = style.font
       
        
        setTitle(text?.uppercased(), for: UIControlState())
        setImage(icon?.tint(style.textColor), for: UIControlState())
        
        self.tag = tag
        self.titleEdgeInsets = titleEdgeInsets
        self.imageEdgeInsets = imageEdgeInsets
        
        if let border = style.border {
            layer.borderColor = border.color.cgColor
            layer.borderWidth = border.width
            layer.cornerRadius = border.radius
            
        }
        
        if let shadow = style.shadow {
            layer.shadowColor   = shadow.color.cgColor
            layer.shadowOpacity = Float(min(1.0, shadow.opacity))
            layer.shadowRadius  = shadow.radius
            layer.shadowOffset  = shadow.offset
            
        }
    }
}
*/
